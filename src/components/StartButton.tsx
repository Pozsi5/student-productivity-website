import React from 'react';

const StartButton: React.FC = () => (
    <button className="mt-8 bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-6 rounded-full text-lg">
        Start
    </button>
);

export default StartButton;